## 0.0.2

* Fix: Crash in `RenderSliverYogaLayout` when scrolling (RenderBox not laid out).
* Fix: Assertion failure `lastIndex >= firstIndex` in Sliver layout.
* Fix: Null check operator error on `layoutOffset`.
* Feat: Add `YogaScrollController` with `scrollToIndex` support.
* Feat: Change default `flexDirection` to `Column`.
